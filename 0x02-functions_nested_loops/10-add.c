#include "main.h"
/**
 * add - adds two integers and returns the result.
 * @a: arguments to be added.
 * @b: argument to be added.
 * Return: Final resut
 */

int add(int a, int b)
{
	int sum;

	sum = (a + b);
	return (sum);
}
