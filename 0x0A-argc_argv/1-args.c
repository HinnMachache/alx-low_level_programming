#include <stdlib.h>
#include <stdio.h>
/**
 * main - prints number of arguments
 * Return: Always 0 success, non 0 - failure
 * @argc: Number of arguments
 * @argv: Parameters
 */

int main(int argc, char *argv[])
{
	printf("%d\n", argc);

	return (0);
}
